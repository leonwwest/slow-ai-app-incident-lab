global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: "slow-ai-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8010"]
        labels:
          service: "slow-ai-app"
          region: "eu-central-1"
